{
  "name": "aurea-monorepo",
  "version": "1.0.0",
  "private": true,
  "devDependencies": {
    "@biomejs/biome":"2.3.15"
  },
  "scripts": {
    "dev": "pnpm --parallel -r dev",
    "build": "pnpm -r build",
    "start": "pnpm --filter @aurea/server start"
  }
}
